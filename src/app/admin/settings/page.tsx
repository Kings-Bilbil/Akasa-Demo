import { createClient } from "@/utils/supabase/server"; import { createAdminClient } from "@/utils/supabase/admin"; import { revalidatePath } from "next/cache"; export default async function AdminSettingsPage() { const supabase = await createClient(); const { data } = await supabase.from("web_settings").select("*").eq("key", "gmaps_iframe_url").single(); const currentUrl = data?.value || ""; async function saveSettings(formData: FormData) { "use server"; const url = formData.get("gmaps_url") as string; const adminSupabase = createAdminClient(); await adminSupabase.from("web_settings").upsert({ key: "gmaps_iframe_url", value: url }); revalidatePath("/"); revalidatePath("/admin/settings"); } return (<div className="max-w-2xl"><h1 className="text-3xl font-bold text-gray-900 mb-8">Pengaturan Web</h1><div className="bg-white p-6 rounded-xl shadow border border-gray-100"><h2 className="text-xl font-bold text-gray-800 mb-4">Lokasi Google Maps (Footer)</h2><p className="text-gray-600 mb-6">Masukkan URL <strong>src</strong> dari iframe Google Maps (Google My Maps atau Google Maps biasa). Ini akan memperbarui peta yang tampil di bagian bawah Halaman Utama.</p><form action={saveSettings} className="space-y-4"><div><label className="block text-sm font-bold text-gray-700 mb-2">URL Google Maps Iframe (src):</label><input type="text" name="gmaps_url" defaultValue={currentUrl} className="w-full p-3 border border-gray-300 rounded-lg text-gray-900 focus:ring-2 focus:ring-blue-500 outline-none" placeholder="https://www.google.com/maps/embed?pb=..." required /></div><button type="submit" className="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition">Simpan Pengaturan</button></form></div></div>); }
