import { createAdminClient } from "@/utils/supabase/admin"; import { notFound } from "next/navigation"; import PrintButton from "./PrintButton"; export default async function InvoicePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const adminClient = createAdminClient(); const { data: order } = await adminClient.from("orders").select("*, customers(full_name, email), order_items(*, products_cache(name)), branches_cache(name)").eq("id", id).single(); if (!order || order.status !== "paid") { notFound(); } return (<div className="max-w-3xl mx-auto p-10 bg-white"><div className="flex justify-between items-start border-b pb-8 mb-8"><div><h1 className="text-4xl font-black text-gray-900 mb-2">INVOICE</h1><p className="text-gray-500 font-mono text-sm">#{order.id.split("-")[0].toUpperCase()}</p></div><div className="text-right"><h2 className="text-xl font-bold text-gray-800">Azuraya B2B</h2><p className="text-gray-500">Cabang Pengambilan: <br/><strong>{order.branches_cache?.name}</strong></p></div></div><div className="flex justify-between mb-10"><div><p className="text-sm text-gray-500 font-bold mb-1">DIBAYAR OLEH:</p><p className="text-lg font-bold text-gray-900">{order.customers?.full_name || "Pelanggan"}</p><p className="text-gray-600">{order.customers?.email}</p></div><div className="text-right"><p className="text-sm text-gray-500 font-bold mb-1">TANGGAL PEMBAYARAN:</p><p className="font-medium text-gray-800">{new Date(order.created_at).toLocaleDateString("id-ID")}</p><p className="text-sm text-gray-500 font-bold mt-4 mb-1">STATUS:</p><p className="text-green-700 font-black tracking-wider border-2 border-green-700 px-3 py-1 rounded inline-block">LUNAS</p></div></div><table className="w-full text-left border-collapse"><thead><tr className="border-b-2 border-gray-800"><th className="py-3 font-bold text-gray-800">Barang</th><th className="py-3 font-bold text-gray-800 text-center">Harga Satuan</th><th className="py-3 font-bold text-gray-800 text-center">Kuantitas</th><th className="py-3 font-bold text-gray-800 text-right">Subtotal</th></tr></thead><tbody>{order.order_items.map((item: any, idx: number) => (<tr key={idx} className="border-b border-gray-200"><td className="py-4 text-gray-800">{item.products_cache?.name}</td><td className="py-4 text-center text-gray-600">Rp {(item.price || 0).toLocaleString("id-ID")}</td><td className="py-4 text-center text-gray-800 font-bold">{item.quantity}</td><td className="py-4 text-right text-gray-800 font-medium">Rp {((item.price || 0) * (item.quantity || 1)).toLocaleString("id-ID")}</td></tr>))}</tbody><tfoot><tr><td colSpan={3} className="py-6 text-right font-bold text-gray-800 text-xl">TOTAL KESELURUHAN</td><td className="py-6 text-right font-black text-gray-900 text-2xl">Rp {(order.total_amount || 0).toLocaleString("id-ID")}</td></tr></tfoot></table><div className="mt-20 border-t pt-8 text-center text-gray-500 text-sm"><p>Terima kasih telah berbelanja di Azuraya B2B.</p><p>Dokumen ini adalah bukti pembayaran yang sah.</p><PrintButton /></div></div>); }
